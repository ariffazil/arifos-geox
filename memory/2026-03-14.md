# Memory — 2026-03-14

## Session Context
- Started a new session using `google/gemini-3-flash-preview` to address configuration issues carried over from 2026-03-13.
- Investigated failures in the `image` analysis tool and the `arifOS.kernel` MCP tool.

## Key Decisions & Learnings
1.  **Image Tool Failure:** The `image` tool is non-functional across multiple providers:
    - OpenAI: 401 (Incorrect API key).
    - Venice: 402 (Insufficient balance).
    - Anthropic: 404 (Not Found/Model issue).
    - Gemini 3 Flash: Reported as not supporting images in the tool response (though the model itself usually does, the OpenClaw integration might be constrained).
2.  **MCP Tool Divergence:** Attempted to call `arifOS.kernel` but received an "Unknown tool" error. The actual MCP tool list shows names like `agentzero_engineer`, `agentzero_memory_query`, `check_vital`, and `vault_seal`.
3.  **Config Location Verified:** Canonical `openclaw.json` confirmed at `/root/.openclaw/openclaw.json`.
4.  **Kimi API Key:** Verified that the key in `openclaw.json` matches the one reported as invalid on 2026-03-13.

## Carry Forward
- **Fix Configuration:** Need valid API keys for Kimi, Brave (for `web_search`), and a functional image provider.
- **Tool Mapping:** Clarify the relationship between `arifOS.kernel` documented stages and the actual MCP tools (`agentzero_*`) exposed by `arifosmcp`.
- **Swarm Test:** Proceed with the NVIDIA market brief test once the foundation (search and reasoning) is verified as stable.
