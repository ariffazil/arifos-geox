# GEOX SOT audit — 2026-09-06

Probe-before-act. Live `/health` beats files.

## Live (OBS)

| Probe | Result |
|---|---|
| `GET :8081/health` | `healthy`, version `v2026.08.26`, `tools_loaded=26`, `canonical_tools=26`, `authority_ceiling=555_COMPUTE_ONLY`, `domain_law=NATURAL_LAW` |
| Unit | `geox-mcp.service`, cwd `/opt/geox`, entry `python3 -m geox_mcp.server` |
| Source HEAD `/root/GEOX` | `e16c95cb` four-tool evidence-path repair (ahead of origin before this wrap) |
| Runtime git `/opt/geox` (pre-deploy) | `42656330` protocol gate — **source/runtime drift** |
| `/tools/list` HTTP | **404** — list is MCP JSON-RPC, not REST |

## Document drift found

| File | Claim | Reality | Action |
|---|---|---|---|
| `GEOX/README.md` | 19 tools, 18 apps, `python -m geox.server`, github `arif-fazil`, FED `:7080`, Docker as live | 26 tools, 12 UI apps, `geox_mcp.server`, `ariffazil`, arifOS `:8088`, systemd `/opt/geox` | **this wrap — README rewritten** |
| `GEOX/DEPLOYMENT.md` | Docker-only, `arif-fazil` clone | Live is systemd | live section added |
| `AAA/docs/ORGAN.md` | GEOX **33** tools; runtime “process from source” | 26; `/opt/geox` | patched |
| `AAA/federation/organs.yaml` | `live_probe_2026_07_30.tools: 33` | dated snapshot, not a lie if left dated | added `live_probe_2026_09_06` |
| `geox_mcp/tool_discovery.py` | “canonical 16” | stale comment vs 26-surface registry | noted, not rewritten this wrap |
| `tools_manifest.yaml` | `public_count_target: 20`, 80 named rows | ghosts + shims; public surface 26 | leave; health is SOT |
| Sibling READMEs (A-FORGE, FRAME, ariffazil) | port `:8081` | still true | no edit |

## What GEOX is not (shadow)

- Not a GNSS / InSAR organ
- Not a live well-pressure historian (no Malay LAS)
- Not APEX and not a seal authority
- GPlates rotation files not loaded (`GEOX_GPLATES_ROT_DIR`)
- Macrostrat v2 can return HTTP 500 — must surface as `ok: false`

## Deploy rule

Copy is not deploy. Deploy = `/root/GEOX` commit on `origin/main` **and** `/opt/geox` at the same SHA **and** `systemctl restart geox-mcp` **and** `/health` 26 tools.
