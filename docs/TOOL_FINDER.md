# arifOS MCP — Tool Finder
## "I want to..." → "Use this tool"

The fastest way to find what you need.

---

## 🚦 Start Here

### "I want to start using arifOS"
→ **`init_anchor`** — Establish a governed session. Do this first.

### "I want to do something and just want approval"
→ **`apex_soul`** — Ask "Is this safe?" Get a final constitutional verdict.

---

## 🔍 Find Truth

### "I want to search the web without hallucinations"
→ **`physics_reality`** — Grounds your query in real data before reasoning.

### "I want to verify a claim is true"
→ **`reality_compass`** — Epistemic intake — checks if your claim matches reality.

### "I want to map a concept to philosophy"
→ **`reality_atlas`** — Maps your query to 27 philosophical zones.

### "I want to estimate computational cost before running"
→ **`math_estimator`** — "How expensive is this operation?"

---

## 🧠 Think & Reason

### "I want deep multi-step reasoning"
→ **`agi_mind`** — Convergent reasoning with entropy minimization. The main reasoning tool.

### "I want logical deduction on a problem"
→ **`agi_reason`** — Builds reasoning chains. Good for structured problems.

### "I want the system to check itself"
→ **`agi_reflect`** — Self-examination. Asks "Are we healthy? Are we sure?"

### "I want adversarial critique before acting"
→ **`asi_critique`** — Red-team review. "What could go wrong?"

### "I want emotional/safety validation"
→ **`asi_heart`** — Safety + empathy check using RASA listening protocol.

### "I want to simulate outcomes before committing"
→ **`asi_simulate`** — "What happens if I do X?" Run scenarios.

---

## 💾 Remember & Forget

### "I want to remember something for later"
→ **`engineering_memory`** — Store with constitutional tagging. Session-scoped.

### "I want to verify memory wasn't tampered"
→ *(coming soon)* — `verify_memory_integrity`

---

## 📋 Record & Audit

### "I want to record a decision permanently"
→ **`vault_ledger`** — Writes to immutable Merkle-chained audit trail.

### "I want to seal/close a session"
→ **`vault_seal`** — Commits session + telemetry to vault.

### "I want to verify a past record is intact"
→ **`verify_vault_ledger`** — SHA-256 chain validation. Check for tampering.

---

## ⚙️ Execute

### "I want to run code safely"
→ **`code_engine`** — Sandboxed execution with constitutional checks.

### "I want to generate a new tool"
→ **`agentzero_engineer`** — Runtime tool generation. All 13 floors activate.

---

## 🔬 Inspect

### "I want to see the entropy state"
→ **`entropy_report`** — Current thermodynamic state of the system.

### "I want to check floor activations"
→ **`floor_status`** — Which F1-F13 floors are currently enforced?

### "I want to see the uncertainty band"
→ **`uncertainty_band`** — Current Ω (Omega) calibration state.

---

## 📊 Tool by Category

### GOVERNANCE (constitutional enforcement)
| Tool | Purpose |
|------|---------|
| `init_anchor` | Start a governed session |
| `vault_ledger` | Record to immutable audit trail |
| `vault_seal` | Commit session to vault |
| `verify_vault_ledger` | Verify record integrity |
| `apex_soul` | Final safety verdict |

### INTELLIGENCE (reasoning & critique)
| Tool | Purpose |
|------|---------|
| `agi_mind` | Deep convergent reasoning |
| `agi_reason` | Logical reasoning chains |
| `agi_reflect` | Self-examination & health check |
| `asi_heart` | Emotional/safety validation |
| `asi_critique` | Adversarial critique |
| `asi_simulate` | Scenario simulation |

### REALITY (grounding in truth)
| Tool | Purpose |
|------|---------|
| `physics_reality` | Web search + factual grounding |
| `reality_compass` | Epistemic intake |
| `reality_atlas` | Philosophical mapping |
| `math_estimator` | Computational cost estimate |

### MEMORY
| Tool | Purpose |
|------|---------|
| `engineering_memory` | Session-scoped memory (Redis) |

### EXECUTION
| Tool | Purpose |
|------|---------|
| `code_engine` | Safe code execution |
| `agentzero_engineer` | Generate new constitutional tools |

---

## 🔑 Common Flows

**Research with verification:**
```
physics_reality → agi_mind → apex_soul → vault_ledger
```

**Safe code execution:**
```
code_engine → apex_soul → vault_ledger → vault_seal
```

**Decision with self-check:**
```
agi_reason → agi_reflect → asi_critique → apex_soul
```

---

**Ditempa Bukan Diberi** — Forged, Not Given.
