# arifOS gitleaks configuration
# Allowlists historical commits where secrets were cleaned up.
# All allowlisted secrets have been ROTATED.

[allowlist]
  description = "Known historical commits with secrets (rotated/remediated)"
  commits = [
    "1dc7cba4978a2c7dc4d214e8978a4afa3489403a",  # auth.json sk-tsfnq redacted in 5c7f862b
  ]
  
  # Also allowlist the specific file path for infrastructure configs
  paths = [
    "AAA/infrastructure/vps_root/opencode-config/auth.json",
  ]
