{
  "mcpVersion": "1.0.0",
  "name": "arifOS",
  "description": "Constitutional AI Governance System — 11 Mega-Tools for governed AI agent operations",
  "tools": [
    {
      "name": "init_anchor",
      "description": "🔥 THE IGNITION STATE OF INTELLIGENCE (Unified). Constitutional session anchor. Modes: init, revoke, refresh, state, status.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["init", "revoke", "refresh", "state", "status"] },
          "actor_id": { "type": "string", "minLength": 2, "maxLength": 64 },
          "intent": { "type": ["string", "object"] },
          "declared_name": { "type": "string", "maxLength": 64 },
          "session_id": { "type": "string", "minLength": 8, "maxLength": 128 },
          "reason": { "type": "string", "maxLength": 1000 },
          "human_approval": { "type": "boolean" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "arifOS_kernel",
      "description": "444_ROUTER: Primary metabolic conductor. Processes complex queries through the 000-999 pipe. Modes: kernel, status.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["kernel", "status"] },
          "query": { "type": "string", "minLength": 1, "maxLength": 40000 },
          "intent": { "type": ["string", "object"] },
          "context": { "type": "string", "maxLength": 100000 },
          "max_steps": { "type": "integer", "minimum": 1, "maximum": 50, "default": 13 },
          "session_id": { "type": "string" },
          "detail": { "type": "string", "enum": ["brief", "full"], "default": "full" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "apex_soul",
      "description": "888_JUDGE: Final authority for verdicts and defense. Modes: judge, rules, validate, hold, armor, notify, probe.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["judge", "rules", "validate", "hold", "armor", "notify", "probe"] },
          "candidate": { "type": "string" },
          "hold_id": { "type": "string" },
          "message": { "type": "string" },
          "session_id": { "type": "string" },
          "target_floor": { "type": "string", "description": "Specific floor to probe (e.g. F12)" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "vault_ledger",
      "description": "999_VAULT: Permanent decision recording and integrity. Modes: seal, verify.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["seal", "verify"] },
          "verdict": { "type": "string" },
          "evidence": { "type": "string" },
          "full_scan": { "type": "boolean", "default": true },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "agi_mind",
      "description": "333_MIND: Core reasoning and synthesis engine. Modes: reason, reflect, forge.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["reason", "reflect", "forge"] },
          "query": { "type": "string" },
          "topic": { "type": "string" },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "asi_heart",
      "description": "666_HEART: Safety, empathy, and consequence modeling. Modes: critique, simulate.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["critique", "simulate"] },
          "content": { "type": "string" },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "engineering_memory",
      "description": "555_MEMORY: Governed autonomous engineering and vector memory. Modes: engineer, vector_query, vector_store, vector_forget, generate, query.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["engineer", "vector_query", "vector_store", "vector_forget", "generate", "query"] },
          "task": { "type": "string" },
          "query": { "type": "string" },
          "prompt": { "type": "string" },
          "content": { "type": "string" },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "physics_reality",
      "description": "111_SENSE: Earth-Witness fact acquisition and mapping. Modes: search, ingest, compass, atlas, time.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["search", "ingest", "compass", "atlas", "time"] },
          "input": { "type": "string" },
          "operation": { "type": "string" },
          "session_id": { "type": "string" },
          "top_k": { "type": "integer", "default": 5 },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "math_estimator",
      "description": "444_ROUTER: Quantitative health and metabolic estimation. Modes: cost, health, vitals.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["cost", "health", "vitals"] },
          "action": { "type": "string" },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "code_engine",
      "description": "M-3_EXEC: System-level hygiene and observation. Modes: fs, process, net, tail, replay.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["fs", "process", "net", "tail", "replay"] },
          "path": { "type": "string", "default": "." },
          "limit": { "type": "integer", "default": 50 },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    },
    {
      "name": "architect_registry",
      "description": "M-4_ARCH: Tool and resource discovery. Modes: register, list, read.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "mode": { "type": "string", "enum": ["register", "list", "read"] },
          "uri": { "type": "string" },
          "session_id": { "type": "string" },
          "auth_context": { "type": ["object", "null"] },
          "risk_tier": { "type": "string", "enum": ["low", "medium", "high", "critical"], "default": "medium" },
          "dry_run": { "type": "boolean", "default": true }
        }
      }
    }
  ],
  "resources": [
    { "uri": "about://arifos", "name": "About arifOS", "description": "High-level overview of the arifOS Constitutional Governance system." },
    { "uri": "canon://floors", "name": "Constitutional Floors", "description": "Detailed specification of the 13 Constitutional Floors (F1-F13)." },
    { "uri": "canon://contracts", "name": "Tool Contracts", "description": "Functional and safety contracts for the 11 Mega-Tools." },
    { "uri": "canon://states", "name": "State Ladder", "description": "Forensic session state progression." },
    { "uri": "arifos://status/vitals", "name": "System Vitals", "description": "Real-time thermodynamic and metabolic health." },
    { "uri": "arifos://caller/state", "name": "Caller State", "description": "Current session identity and authority level." },
    { "uri": "arifos://sessions/{session_id}/vitals", "name": "Session Vitals", "description": "Dynamic vitals for a specific governed session.", "isTemplate": true }
  ],
  "prompts": [
    { "name": "init_anchor", "description": "Prompt for establishing a constitutional session." },
    { "name": "arifOS_kernel", "description": "Prompt for the primary metabolic conductor." },
    { "name": "agi_mind", "description": "Prompt for first-principles reasoning and reflection." },
    { "name": "asi_heart", "description": "Prompt for ethical simulation and adversarial critique." },
    { "name": "apex_soul", "description": "Prompt for sovereign judgment and safety auditing." },
    { "name": "vault_ledger", "description": "Prompt for immutable recording and verification." },
    { "name": "physics_reality", "description": "Prompt for grounding thoughts in the physical world." },
    { "name": "code_engine", "description": "Prompt for OS-level hygiene and computational execution." }
  ]
}
