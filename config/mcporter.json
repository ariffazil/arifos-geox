{
  "mcpServers": {
    "context7": {
      "_comment": "Context7 — Up-to-date library docs for LLMs. Remote HTTP, no API key needed for free tier.",
      "url": "https://mcp.context7.com/mcp",
      "transport": "streamable-http"
    },
    "codegraphcontext": {
      "_comment": "CodeGraphContext — Codebase graph analysis. Local stdio via pip-installed cgc.",
      "command": "cgc",
      "args": ["mcp", "start"],
      "transport": "stdio"
    },
    "arifos": {
      "_comment": "arifOS MCP kernel — internal Docker service on the OpenClaw network.",
      "url": "http://arifosmcp:8080/mcp",
      "transport": "streamable-http"
    }
  }
}
